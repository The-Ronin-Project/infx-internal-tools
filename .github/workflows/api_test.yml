name: Hashi API Unit Tests

on:
  pull_request:

jobs:
  python-test:
    uses: projectronin/github/.github/workflows/python_test.yml@master
    secrets:
      codecov-token: ${{ secrets.HASHI_CODECOV_TOKEN  }}
