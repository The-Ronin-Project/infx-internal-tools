apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: {{ .Release.Namespace }}
  name: {{ include "hashi.fullname" . }}
  labels:
    app: {{ template "hashi.fullname" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
spec:
  replicas: {{ .Values.deployment.replicas }}
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: {{ template "hashi.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ template "hashi.fullname" . }}
        chart: {{ .Chart.Name }}-{{ .Chart.Version }}
      annotations:
    spec:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: {{ template "hashi.fullname" . }}
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/node
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: {{ template "hashi.fullname" . }}
      containers:
        - name: hashi
          image: {{ .Values.image.repo }}:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          resources:
            requests:
              cpu: {{ .Values.resources.requests.cpu }}
              memory: {{ .Values.resources.requests.memory }}
            limits:
              memory: {{ .Values.resources.limits.memory }}
          ports:
            - containerPort: 80
          env:
            {{- include "hashi.env" . | nindent 10 }}
          livenessProbe:
            initialDelaySeconds: 30
            httpGet:
              path: /ping
              scheme: HTTP
              port: 80
            timeoutSeconds: 2
          readinessProbe:
            initialDelaySeconds: 30
            httpGet:
              path: /ping
              scheme: HTTP
              port: 80
            timeoutSeconds: 2
      restartPolicy: Always